# evm-bridge-relayer-core
A decentralized off-chain relayer node for EVM-compatible cross-chain bridges. Listens for 'Lock' events on the source chain, validates merkle proofs, and executes 'Release' transactions on the destination chain using ECDSA signatures.
